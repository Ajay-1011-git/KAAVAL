{
  "name": "@kaaval/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "KAAVAL browser SDK — WebAuthn ceremonies, non-exportable session key generation, request canonicalization + signing (PulseLock client half).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
