# backend/chronicle/routes.py — Stage 0 stub
#
# Owned by Sai (feature/dashboard-chronicle). Empty placeholder router so
# backend/main.py has something real to mount from Stage 0 onward; Sai's
# branch replaces its contents (POST /chronicle/explain, per TRD §5 and
# T-SA.8). Do not build out Chronicle's actual logic here.

from fastapi import APIRouter

router = APIRouter()
