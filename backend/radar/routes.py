# backend/radar/routes.py — Stage 0 stub
#
# Owned by Adhi (feature/radar-guardian). This is an empty placeholder
# router so backend/main.py has something real to mount from Stage 0
# onward; Adhi's branch replaces its contents (GET /radar/report, per
# TRD §5 and T-AD.4). Do not build out Radar's actual logic here.

from fastapi import APIRouter

router = APIRouter()
